// Web stub — expo-sqlite is not supported on web.
// All functions are no-ops so that the web bundle doesn't try to load the SQLite worker.

export async function getDb(): Promise<any> {
  return null;
}
